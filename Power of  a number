#include<iostream>
#include<cmath>
using namespace std;
int main()
{
    float num,power,a;
    float res;
    cin>>num>>power;

    if(power>=0)
    {
    res=pow(num,power);
    }
    else
    {
        power=-1*power;
        a=pow(num,power);
        res=1/a;
     
    }
    cout<<res;
}
